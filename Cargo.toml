[package]
name = "cuda-learning"
version = "0.1.0"
edition = "2021"
description = "Experience-based learning — agents improve behavior from outcomes using credit assignment and generalization"

[dependencies]
serde = { version = "1", features = ["derive"] }
